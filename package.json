{
  "name": "clearmandate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Policy-bound, verified payments for AI agents.",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

